============
Installation
============

At the command line::

    $ easy_install django-external

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-external
    $ pip install django-external
